{
	"name": "react-parcel",
	"version": "1.0.0",
	"description": "Boilerplate for building react apps with parcel-bundler",
	"main": "index.js",
	"scripts": {
		"start": "rm -rf dist && rm -rf .cache && parcel index.html",
		"build": "rm -rf dist && rm -rf .cache && parcel build index.html --no-source-maps"
	},
	"author": "ashiqsultan",
	"license": "MIT",
	"dependencies": {
		"axios": "^0.21.1",
		"react": "^16.13.1",
		"react-dom": "^16.13.1"
	},
	"devDependencies": {
		"@babel/core": "^7.9.0",
		"@babel/preset-env": "^7.9.5",
		"@babel/preset-react": "^7.9.4",
		"parcel-bundler": "^1.12.4",
		"parcel-plugin-bundle-visualiser": "^1.2.0"
	}
}
